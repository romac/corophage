[package]
name    = "corophage"
version = "0.1.0"
edition = "2024"

[dependencies]
fauxgen    = { version = "0.1" }
frunk_core = { version = "0.4" }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
