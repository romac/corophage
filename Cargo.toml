[package]
name    = "corophage"
version = "0.1.0"
edition = "2021"

[dependencies]
genawaiter = { version = "0.99.1", default-features = false }
frunk      = { package = "frunk_core", version = "0.4" }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
