default: build

# Build the site and generate llms.txt files
build:
    zola build
    python3 scripts/generate_llms_txt.py
    mkdir -p public/.well-known
    ln -sf ../llms.txt public/.well-known/llms.txt

# Serve the site locally
serve:
    zola serve
